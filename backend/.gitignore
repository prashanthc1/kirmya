# Binaries
/bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Build output
*.out
*.test

# Air live-reload build artifacts (Docker dev)
/tmp/

# Editor files
.vscode/
.DS_Store

# Environment files
.env

# The compiled server binary (built at the backend root). Anchored with a
# leading slash so it does not also match the cmd/workspace-app/ source package.
/workspace-app
coverage
coverage.html
